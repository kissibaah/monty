#include "monty.h"
/**
 * f_stack - prints the top
 * @head: stack head
 * @ln_num: line_number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int ln_num)
{
	(void)head;
	(void)ln_num;
	bus.lifi = 0;
}
